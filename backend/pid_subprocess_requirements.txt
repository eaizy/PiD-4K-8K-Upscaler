# Dependencies for the PiD reference-CLI subprocess venv (Scale-RAE 8× backbone).
#
# These are installed by `installer.ensure_scale_rae` into a DEDICATED venv at
# <PID_HOME>/pid-venv — kept separate from the lean backend venv. PyTorch is
# installed separately first (CUDA build, see PID_TORCH_INDEX_URL), then the
# cloned nv-tlabs/PiD repo is installed editable (`pip install -e`).
#
# Ported from the original Uniqlaw pid-sidecar requirements (the transitive deps
# the PiD repo imports but does not always declare in its own setup).

transformers>=4.57.0
diffusers>=0.37.0
safetensors>=0.4.5
einops>=0.8.0
huggingface_hub>=0.26.0
omegaconf>=2.3.0
hydra-core>=1.3.2
loguru>=0.7.2
opencv-python-headless>=4.10.0
imageio>=2.36.0          # PiD inference_utils.py import
attrs>=24.0.0            # PiD imaginaire config dataclass
sentencepiece>=0.2.0     # transformers tokenizer
pandas>=2.2.0            # PiD demo manifest reader
termcolor>=2.4.0         # PiD imaginaire logging
fvcore>=0.1.5            # PiD imaginaire model utils
iopath>=0.1.10           # PiD imaginaire checkpoint IO
packaging>=24.0
wandb>=0.18.0            # imaginaire.utils.misc top-level import
boto3>=1.35.0            # lazy upload (S3) — import-time check
botocore>=1.35.0
pillow>=11.0.0
numpy>=2.1.0
